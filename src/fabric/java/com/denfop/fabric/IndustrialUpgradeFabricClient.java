package com.denfop.fabric;

import net.fabricmc.api.ClientModInitializer;

public final class IndustrialUpgradeFabricClient implements ClientModInitializer {
    @Override
    public void onInitializeClient() {
        // Client-only screens, renderers, keybinds and model registration will be
        // migrated here. No client class is referenced from the common entrypoint.
    }
}
